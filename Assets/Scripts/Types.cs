using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class Types
{
    public enum ColorTypes { blue, pink, green, yellow, purple, red };
    public enum IconTypes { normal, A, B, C };
}
